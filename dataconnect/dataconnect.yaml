specVersion: "v1beta"
serviceId: "e-comm"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "e-comm-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
